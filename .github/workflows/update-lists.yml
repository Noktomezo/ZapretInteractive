name: Update Thirdparty

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-thirdparty
  cancel-in-progress: true

jobs:
  update-thirdparty:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: '3.12'
          enable-cache: true

      - name: Sync managed thirdparty files and hashes
        run: uv run scripts/update-thirdparty.py

      - name: Verify thirdparty hashes
        run: uv run scripts/verify-thirdparty-hashes.py

      - name: Check for thirdparty changes
        id: check_changes
        run: |
          if git status --porcelain --untracked-files=all --ignored=no -- thirdparty | grep -q .; then
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add thirdparty
          git commit -m "chore: update managed thirdparty files"
          git push
